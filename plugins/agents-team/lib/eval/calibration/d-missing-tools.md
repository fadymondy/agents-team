---
name: tools-omitted-agent
description: "Edits the codebase, runs commands, and does whatever is needed. Use when something needs doing."
model: opus
---

# Tools-omitted Agent

This agent has no `tools:` line in its frontmatter, so it silently inherits every tool from its parent — the most common over-grant anti-pattern.
