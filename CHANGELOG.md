# Changelog

All notable changes to this project are documented here. This project follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/) and [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

The file is regenerated by [git-cliff](https://github.com/orhun/git-cliff) on every push to `main`. Hand-edits between releases will be overwritten — write conventional-commit messages instead.

## [Unreleased]


### Added

- Harden /team-gen self-eval gate (env-gate + --min-grade) (`8693bc1`)
- Complete archetype fixtures + e-Shop example PRD + scaffolded team (`52b9ed7`)

### Documentation

- Update CHANGELOG [skip ci] (`03f23b7`)
- Update CHANGELOG [skip ci] (`237101f`)
- Update CHANGELOG [skip ci] (`e9cc037`)

### Repo plumbing

- Repo hygiene — CONTRIBUTING + SECURITY + requirements + editorconfig (`58789a1`)
- Post-release bump to 0.2.0-dev + release workflow + ritual docs (`57607f7`)
## [0.1.0] - 2026-04-29


### Added

- Documentation — README + per-pillar guides + contributor doc (`840b460`)
- /team-gen skill — generate a complete agent team from a PRD (`a25f967`)
- Behavioral harness — Phase 3 (skeleton, replay-only) (`4b230aa`)
- Rubric document + LLM-as-judge — Phase 2 (skeleton) (`20e37eb`)
- /meet skill — streamed multi-agent team meeting (`a76ffb6`)
- Static linter — Phase 1 (`deaaaf5`)
- Agent + skill templates library (`92b05b8`)
- Numbered rules library (13 templates) (`8934301`)
- Port hooks library (`003c903`)
- Output schema v1 + Markdown renderer (`8f1e629`)
- Plugin scaffolding (`8ec1760`)

### Documentation

- Add CHANGELOG with auto-update workflow + sponsor link in README (`31d3e22`)

### Repo plumbing

- Add GitHub Sponsors funding link for @fadymondy (`368ab70`)
- Bootstrap gh-pms templates and labels (`fb70ec7`)

